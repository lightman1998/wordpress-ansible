- hosts: all
  connection: local
  tasks:
  - name: install python 2
    raw: test -e /usr/bin/python || (apt -y update && apt install -y python-minimal)

- hosts: wordpress
  connection: local
  roles:
    - server
    - php
    - mysql
    - wordpress
