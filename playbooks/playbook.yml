- hosts: all
  connection: local
  tasks:
  - name: install python 2
    package:
      name:
        - python-minimal
      state: present

- hosts: wordpress
  connection: local
  roles:
    - server
    - php
    - mysql
    - wordpress
